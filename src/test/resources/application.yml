spring:
  application:
    name: wallet
  jpa:
    properties:
      hibernate:
        hbm2ddl.auto: update
        dialect: org.hibernate.dialect.PostgreSQLDialect

  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_ON_EXIT=FALSE;
    username: "admin"
    password: ""
  liquibase:
    enabled: false
